/* Contains constants used in the project. */
interface TokenConstants {
  ACCESS_TOKEN: string;
  REFRESH_TOKEN: string;
}

export const TOKEN_CONSTANTS: TokenConstants = {
  ACCESS_TOKEN: 'accessToken',
  REFRESH_TOKEN: 'refreshToken',
};
